import React from 'react';

describe('Engagement Form Context', () => {
  test('saveChanges should call engagementContext.saveEngagement', () => {
    expect(true).toBe(false);
  });
  test('can register a field group', () => {
    expect(true).toBe(false);
  });
  test('can register a field', () => {
    expect(true).toBe(false);
  });
  test('can update engagement fields', () => {
    expect(true).toBe(false);
  });
  test('saving an engagement should clear the current changes', () => {
    expect(true).toBe(false);
  });
  test('calling clearCurrentChanges will clear the current changes', () => {
    expect(true).toBe(false);
  });
});
